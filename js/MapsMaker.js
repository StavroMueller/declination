//This is a multipurpose google maps creator, with a whole bunch of game type functions built in.
// Mostly cause I don't want to do this same stuff over and over.
var mapsMaker = {};

(function() {
	mapsMaker.makeGoogleMap = function(divId)
	{
		var mapOptions = {
			mapTypeId: google.maps.MapTypeID.TERRAIN,
			zoom:8


		};
		//some stuff here

	};

})()
